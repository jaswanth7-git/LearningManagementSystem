spring.application.name=LearningPortal
# ========================
# = DATABASE CONFIGURATION =
# ========================
spring.datasource.url=jdbc:mysql://localhost:3306/LearningPortal?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

spring.datasource.username=root
spring.datasource.password=Jaswanth@77
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========================
# = JPA / HIBERNATE CONFIG =
# ========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ========================
# = SERVER CONFIG =
# ========================
server.port=8080

# ========================
# = LOGGING (Optional) =
# ========================
logging.level.org.springframework.web=DEBUG
logging.level.com.yourcompany.lms=DEBUG

# = All other Configs
#server.servlet.context-path=/lms/api this can add /lms/api to all your endpoints and static files also